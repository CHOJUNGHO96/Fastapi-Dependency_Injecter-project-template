뉴스조회: |
  SELECT
      article_id,
      title,
      content,
      source,
      url  
  FROM
      news_articles
  WHERE TRUE
  {% if article_id %}
    AND article_id = {{ article_id }}
  {% endif %}        
